all:
  children:
    mydocker: 
      hosts: 
        10.0.1.10:
  vars:
    ansible_user: ubuntu 
    ansible_ssh_private_key_file: "/home/ubuntu/.ssh/New-Key"
    ansible_ssh_common_arg: "-o StrictHostKeyChecking=no"